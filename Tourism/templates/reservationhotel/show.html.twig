{% extends 'base.html.twig' %}

{% block title %}Reservationhotel{% endblock %}

{% block body %}
    <h1>Reservationhotel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdRes</th>
                <td>{{ reservationhotel.idRes }}</td>
            </tr>
            <tr>
                <th>Datedeb</th>
                <td>{{ reservationhotel.datedeb ? reservationhotel.datedeb|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datefin</th>
                <td>{{ reservationhotel.datefin ? reservationhotel.datefin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Nbrplace</th>
                <td>{{ reservationhotel.nbrplace }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ reservationhotel.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservationhotel_index') }}">back to list</a>

    <a href="{{ path('reservationhotel_edit', {'idRes': reservationhotel.idRes}) }}">edit</a>

    {{ include('reservationhotel/_delete_form.html.twig') }}
{% endblock %}
